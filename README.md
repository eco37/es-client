# es-client

```
usage: es-client.py [-h] [-H HOSTNAME] [-p PORT] [-c COMMANDS [COMMANDS ...]]

optional arguments:
  -h, --help            show this help message and exit
  -H HOSTNAME, --hostname HOSTNAME
                        IP or Hostname (default: localhost)
  -p PORT, --port PORT  Port (default: 9200)
  -c COMMANDS [COMMANDS ...], --commands COMMANDS [COMMANDS ...]
                        Commands
```
